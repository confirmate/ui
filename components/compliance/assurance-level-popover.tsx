import { SchemaCatalog } from "@/lib/api/orchestrator";

interface AssuranceLevelPopover {
  catalog: SchemaCatalog;
  children: React.ReactNode;
}

export default function AssuranceLevelPopover({
  catalog,
  children,
}: AssuranceLevelPopover) {
  return <></>;
}
